import { WidgetDetailsComponent } from './widget-details.component';

describe('Component: WidgetDetails', () => {
  it('should create an instance', () => {
    const component = new WidgetDetailsComponent();
    expect(component).toBeTruthy();
  });
});
