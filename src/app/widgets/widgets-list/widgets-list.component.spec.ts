import { WidgetsListComponent } from './widgets-list.component';

describe('Component: WidgetsList', () => {
  it('should create an instance', () => {
    const component = new WidgetsListComponent();
    expect(component).toBeTruthy();
  });
});
